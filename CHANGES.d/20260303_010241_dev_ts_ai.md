- Removed `check_package_is_module` attribute (broken since 2013, never worked correctly).
- Migrated from `pkg_resources` to `importlib.metadata` for entry point discovery.
- Removed `importlib_metadata` and `importlib_resources` dependencies (stdlib in Python 3.8+/3.9+, project requires 3.10+).
